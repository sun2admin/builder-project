"""Phase 4 — multi-repo analysis aggregation.

Reads per-repo analysis.json files, merges per the rules in plan §1.
Writes builds/<build_name>/aggregated.json.
"""

from __future__ import annotations


def aggregate_analyses(analysis_paths: list) -> dict:
    """Merge a list of analysis.json contents into one aggregated.json shape.

    Stub. See plan §1 merge rules table.
    """
    raise NotImplementedError("aggregate.aggregate_analyses is a stub.")
